apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CAPI controller operators — previously installed by clusterctl during bootstrap,
# now managed by ArgoCD so they can be upgraded via git push.
# Each subdirectory references the upstream provider's config/default with
# image overrides to pin the correct release image.
resources:
  - capi-core
  - caph
  - cabpt
  - cacppt
  - caaph
